[project]
name = "feishu-mcp-server"
version = "0.1.0"
description = "Feishu Open Platform MCP Server — 飞书全功能 MCP 工具集"
requires-python = ">=3.11"
dependencies = [
    "mcp[cli]>=1.27.2",
    "python-dotenv>=1.2.2",
    "requests>=2.34.2",
]

[project.scripts]
feishu-mcp = "server:main"
